coverage:
  status:
    project:
      default:
        target: auto   # never regress below current main coverage
        threshold: 1%  # allow 1% noise before failing
    patch:
      default:
        target: 95%    # new/changed lines in the PR must be ≥80% covered

ignore:
  - "test/**"
  - "bench/**"
